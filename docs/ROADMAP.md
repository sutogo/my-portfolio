# ポートフォリオ 改善ロードマップ

> 作成日: 2026-03-02  
> ブランチ: `feature/redesign`（プッシュ済み）  
> 前回の作業: SPA化・デザインシステム刷新・全セクション改善・研究内容詳細化・LinkedIn追加

---

## ⭐⭐⭐ 最優先（採用インパクト大）

### 1. 数字のメトリクスカード可視化
- About セクション冒頭にカウントアップ風のメトリクスカードを配置
- 表示する数字:
  - `57%` 測定工数削減（月間160時間）
  - `10+` 新規製造ライン立ち上げ
  - `WCB 2026` 国際学会ポスター発表
  - `3` カ国での活動経験（日本・シンガポール・カナダ予定）
- IntersectionObserverでスクロール時にアニメーションカウントアップ

### 2. Future Vision セクション新設
- Contactの手前に配置
- AWS IoT × センサ → CPS社会実装のビジョンを表現
- 「エッジからクラウドまで一気通貫」のコンセプト図（簡易ダイアグラム）
- データ: `docs/profile.md` の「Future Vision」節を参照

### 3. セクション間の視覚的区切り強化
- 偶数セクションに微妙に異なる背景色（例: `--color-bg-secondary`）
- または、セクション間にグラデーションのディバイダーライン
- App.css の `section:nth-child(even)` で制御

---

## ⭐⭐ 優先度中（差がつく）

### 4. プログレスバーのスクロールアニメーション
- SkillCardのプログレスバーが `width: 0` → スクロールインで指定幅まで伸びる
- IntersectionObserverベース（FadeIn同様の仕組み）

### 5. Works セクションの情報厚み
- TKAシステムとADASシミュレーションの詳細を充実させる
  - システム構成図（簡易図でも可）
  - 開発プロセス: 課題発見 → 設計 → 実装 → 検証
  - 技術的チャレンジと解決策
- 画像のないプロジェクト（数理計算プログラムなど）の画像追加 or 削除判断

### 6. ヒーロー3Dモデルのキャプション追加
- 「研究対象の膝関節 3Dモデル」などの小さなキャプションを追加
- 初見の人にも何のモデルか伝わるように

### 7. マイクロインタラクション追加
- ナビバーのアクティブリンクにスムーズなアンダーライン遷移
- CTAボタン「View My Work」にパルスアニメーション（微小な拍動）
- カードホバー時のチルト効果（CSS perspective + transform）

---

## ⭐ 優先度低（あるとプレミアム）

### 8. PDF CV ダウンロードボタン
- ナビバーまたはContactに「Download CV」ボタン
- PDFは `public/` フォルダに配置

### 9. 英語版切り替え
- 最低限: セクションタイトル + About の英語版
- i18n ライブラリ不要、シンプルな state 切替で対応可能
- グローバル志向のアピールになる

### 10. ダークモード / ライトモード切り替え
- CSS変数の差し替えで対応（`:root` と `[data-theme="light"]`）
- ナビバーにトグルボタン
- localStorage で選択を永続化

### 11. パーティクル / ドットグリッド背景
- ヒーローセクションの背景に微細なパーティクルアニメーション
- Canvas API or CSS で実装（Three.js は既にあるので活用可能）

---

## 未対応事項メモ
- `main` ブランチへのマージ → Vercel自動デプロイはまだ未実施
- 旧Layoutコンポーネント・HamburgerMenuは使われなくなったが削除していない（クリーンアップ対象）
- `react-router-dom` はSPA化で不使用になったが `package.json` に残っている（アンインストール候補）
